<Window x:Class="LogVision.Views.EditLogPackageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑日志包"
        Height="340" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundDarkBrush}"
        Foreground="{DynamicResource ForegroundPrimaryBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="编辑日志包" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"
                   Foreground="{DynamicResource ForegroundPrimaryBrush}"/>

        <!-- Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="日志名称" Margin="0,0,0,4" FontWeight="SemiBold"/>
            <TextBox x:Name="TxtName" Margin="0,0,0,10"/>

            <TextBlock Text="描述 (可选)" Margin="0,0,0,4" FontWeight="SemiBold"/>
            <TextBox x:Name="TxtDescription" Height="60" AcceptsReturn="True"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="确认" Width="80" Style="{StaticResource AccentButton}" Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
